# alist

repo：https://github.com/alist-org/alist.git

1. `sudo docker-compose -p 02-alist -f /home/server/02-alist/alist.yml up -d` 启动后，在 log 里看默认账密 `admin / xxxxxx`
2. 在访问页面输入账密登陆后，配置 alist
   1. `个人资料`，修改默认账密