# network scripts

type `pip install -r 'requirement.txt'` before use python scripts

## usage

1.   tcpdump：`python 01-tcpdump.py --port 2333 --interface eth0 `，不加 interface 或 address 默认监听全部扫描到的网卡，windows 下不好用
1.   